# Gitignore for the entire project
# =================================

# Node.js specific ignores
node_modules/
.env
npm-debug.log
yarn-debug.log
yarn-error.log
.pnpm-debug.log*
.npm/
.yarn/
# runtime log files
*.log
log/

# macOS specific ignores
.DS_Store
.localized
.Spotlight-V100
.Trashes

# Windows specific ignores
Thumbs.db
ehthumbs.db
Desktop.ini

# IDE specific ignores (e.g., VS Code)
.vscode/
*.sublime-project
*.sublime-workspace
*.idea/
.history/

# Build artifacts and compiled files (if not in sub-folders)
dist/
build/
.next/ # For Next.js

# =================================
# Sub-folder specific ignores
# =================================

# Frontend specific ignores (e.g., inside frontend/)
/frontend/node_modules/
/frontend/dist/
/frontend/build/
/frontend/.env # If frontend has its own .env
/frontend/.vscode/ # If frontend has its own IDE config

# Backend specific ignores (e.g., inside backend/)
/backend/node_modules/
/backend/dist/
/backend/build/
/backend/.env # If backend has its own .env
/backend/.vscode/ # If backend has its own IDE config
/backend/logs/ # Example: if your backend writes log files